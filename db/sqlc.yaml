version: 2
sql:
  - engine: "sqlite"
    schema: "schema.sql"
    queries: 
      - "queries/credential.sql"
      - "queries/message.sql"
    gen:
      go:
        package: "query"
        out: "query"
        emit_json_tags: true
        emit_prepared_queries: true
        overrides:

