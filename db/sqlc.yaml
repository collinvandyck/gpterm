version: 2
sql:
  - engine: "sqlite"
    schema: "schema.sql"
    queries: 
      - "queries/credentials.sql"
    gen:
      go:
        package: "query"
        out: "query"
        emit_json_tags: true

